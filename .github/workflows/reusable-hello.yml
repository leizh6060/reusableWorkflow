name: Reusable Hello World

on:
  workflow_call:
    inputs:
      username:
        required: true
        type: string

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Greet the user
        run: echo "Hello ${{ inputs.username }}!"
